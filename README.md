# dataplatformos

Data Platform OS is an operating system for data platforms that abstracts everything that needs to work in any platform, abstracts it away from charactersitics of individual platform foundations and implements them using mostly open source. It's a design time tool that runs everything via open source tech (which themselves run on top of the hyperscaler platforms/engines)